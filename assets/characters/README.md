# Character assets

The live game uses a **colored dot** avatar only (trail color from **Skin** in the lobby or Admin house). This folder is reserved for any future sprite work; nothing here is required at runtime.
